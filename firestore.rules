rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // Пользователь может читать и изменять только свой собственный документ
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
